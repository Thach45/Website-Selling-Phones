extends ../../layouts/default.pug
include ../../mixins/filters.pug
include ../../mixins/search.pug
include ../../mixins/pagination.pug
include ../../mixins/alert.pug

block main 
     h1 Các blog liên kết ngoài
     .card.mb-3
            .card-body  
                .row   
                    .col-8
                    .col-4
                        a(
                            href="blog/create"
                            class="btn btn-outline-success"
                        ) + Thêm mới
     table(
            class="table table-hover table-sm"
            checkbox-multi
     ) 
        thead 
            th STT
            th Hình ảnh 
            th description
            th Ngày tạo
            th Hành động

        tbody 
          each item, index in blogs
            tr 
                td #{index+1}
                td 
                    img(src = item.thumbnail, height="auto", width="100px")
                td #{item.description}
                td #{item.created}
                td 
                    form(action=`/admin/news/blog/delete/${item._id}`, method="post")
                        button(
                            type="submit",
                            class="btn btn-danger btn-sn ml-2",
                            data-id=item.id
                        ) Xoá

                        